<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="UniteApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="webActivity" />
        <activity android:name="MonthChartActivity" />
        <activity android:name="SettingActivity" />
        <activity android:name="HistoryActivity" />
        <activity android:name="AboutActivity" />
        <activity android:name="SearchActivity" />
        <activity android:name="RecordActivity" />
        <activity android:name="vtc.oldcookie.paymark.forum.login" />
        <activity android:name="vtc.oldcookie.paymark.forum.Forum" />

        <activity android:name="vtc.oldcookie.paymark.forum.createac" />
        <activity android:name="vtc.oldcookie.paymark.forum.recommand" />
        <activity android:name="vtc.oldcookie.paymark.forum.ForumInput" />
        <activity android:name="vtc.oldcookie.paymark.forum.Bell" />
        <activity
            android:name="vtc.oldcookie.paymark.forum.Settings" />

        <activity
            android:name="MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>